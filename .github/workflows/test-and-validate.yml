name: 🧪 Test & Validate Bot

# Run comprehensive tests before enabling trading
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    # GLOBAL JOB ENV: Makes these available to EVERY step below
    env:
      ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
      ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
      ALPACA_BASE_URL: ${{ secrets.ALPACA_BASE_URL || 'https://paper-api.alpaca.markets' }}

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: 📦 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # Note: Ensure your requirements.txt has 'numpy<2' if using pandas 2.1.0
          pip install -r requirements.txt

      - name: 🔍 Lint Code
        run: |
          echo "Running basic syntax checks..."
          python -m py_compile *.py
          echo "✅ All Python files compile successfully"

      - name: 🧪 Run Unit Tests
        run: |
          echo "Running test suite..."
          # Inherits env from job level
          python test_bot.py

      - name: 🎯 Validate Configuration
        run: |
          python -c "
          import os
          from config import settings

          print('✅ Configuration loaded successfully')
          print(f'   Max Positions: {settings.max_positions}')
          print(f'   Position Size: {settings.max_position_size * 100}%')
          print(f'   Stop Loss: {settings.stop_loss_pct * 100}%')
          print(f'   Confidence Threshold: 70%')
          "

      - name: 📊 Validate Data Files
        run: |
          echo "Checking data directory structure..."
          mkdir -p data logs
          touch data/.gitkeep
          touch logs/.gitkeep
          echo "✅ Data directory structure validated"

      - name: 🧪 Market Connection Test
        if: github.event_name == 'workflow_dispatch'
        run: |
          python -c "
          from alpaca_client import AlpacaClient
          from market_data import get_market_data
          from stock_screener import turbo_screener

          print('🔌 Testing Alpaca connection...')
          client = AlpacaClient()
          account = client.get_account()
          print(f'✅ Account: ${account[\"portfolio_value\"]}')

          print('📊 Testing market data...')
          data = get_market_data(['AAPL'], days=5)
          if not data.empty:
              print(f'✅ Market data: {len(data)} bars retrieved')

          print('🔍 Testing stock screener...')
          opportunities = turbo_screener(max_symbols=5, min_confidence=0.70)
          print(f'✅ Screener found {len(opportunities)} opportunities')

          print('')
          print('✅ All connections validated!')
          "

      - name: ✅ Success Message
        if: success()
        run: |
          echo ""
          echo "=========================================="
          echo "✅ ALL TESTS PASSED"
          echo "=========================================="
          echo ""
          echo "🤖 The trading bot is ready to deploy!"
          echo "=========================================="

      - name: ❌ Failure Message
        if: failure()
        run: |
          echo ""
          echo "=========================================="
          echo "❌ TESTS FAILED"
          echo "=========================================="
          echo ""
          echo "Please review the errors above and fix before deploying."
          echo "=========================================="
